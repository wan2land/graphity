{
  "name": "todo-server",
  "private": true,
  "scripts": {
    "dev": "env-cmd -e dev nodemon --exec \"npm run build && node dist/entry.js\"",
    "start": "npm run build && node dist/entry.js",
    "build": "rimraf dist && tsc"
  },
  "dependencies": {
    "@graphity/server-express": "^0.8.0-alpha.7",
    "@openauth/github": "^0.3.0",
    "env-cmd": "^10.1.0",
    "graphity": "^0.8.0-alpha.7",
    "jsonwebtoken": "^8.5.1",
    "mysql2": "^2.2.5",
    "typeorm": "^0.2.30"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^8.5.0",
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2",
    "typescript": "^3.4.5"
  }
}
